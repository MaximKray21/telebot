some commands: 

git init
git clone
git config --global user.name / user.email "...:

git remote add origin url

git status
git add
git commit -m
git log
git checkout hash

git push origin
git branch -M main